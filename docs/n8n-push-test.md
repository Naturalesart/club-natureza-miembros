# Test de push via n8n

Este archivo confirma que la conexion directa GitHub API (bypass del conector MCP de solo lectura) funciona correctamente.

Se puede borrar sin problema, no afecta ninguna funcionalidad del plugin.
